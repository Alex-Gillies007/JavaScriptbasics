// variables are named containers for stuff

// let is a keyword to declare variables
// age is the name of my variable
//16 is the value to variable age

let age = 14; // the data type of age is a number

let name = "Alex Gillies"; // the data type of fullname is string

let isactive = true; // the data type is isactive is boolean (true or false)

age = false; // age is now a boolean

const MAX_AGE = 200;

var visibleEverywhere = true;

var leg = 16;

let bar = 10;

console.log(leg, bar);

{
	let bar = 20;

	var leg = 32;
	console.log(leg, bar);
}
